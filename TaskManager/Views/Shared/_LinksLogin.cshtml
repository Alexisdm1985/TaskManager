@using Microsoft.AspNetCore.Identity;
@inject SignInManager<IdentityUser> signInManager

<ul class="navbar-nav">
    @if (signInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            <span class="nav-link text-dark">Hola, @User.Identity.Name</span>
        </li>

        <li class="nav-item">
            <form class="form-inline" asp-controller="Usuarios" asp-action="Logout">
                <button type="submit" class="nav-link btn btn-link text-dark">
                    Logout
                </button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Usuarios" asp-action="Registro">
                Registro
            </a>
        </li>

        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Usuarios" asp-action="Login">
                Login
            </a>
        </li>
    }
</ul>

@*
NOTA:
en la linea 5 "User", existe por defecto en alguna parte de dotnet,
por eso no debemos hacer nada mas que llamar a User para obtener
el usuario actual de la app. Ademas, por este hecho puede ser llamado sin
problemas desde cualquier vista de razor.
*@