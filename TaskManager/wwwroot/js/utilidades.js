// Se obtienen las constantes desde la vista parcial _TraduccionTextoJs.cshtml
// el cual contiene el script con la creacion de las constantes globales

function mostrarError() {
    alert(ALERT_MENSAJE_ERROR);
}